// info_detail_page.dart
import 'package:flutter/material.dart';

class InfoDetailPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Detail Informasi'),
      ),
      body: Center(
        child: Text('Info Detail Page'),
      ),
    );
  }
}
